-- Title: Pyright LSP Settings
-- Desc: Configurations that can be made to pyright (an lsp), are 
return {
	settings = {
    python = {
      analysis = {
        typeCheckingMode = "off",
      }
    },

	},
}
